//dotenv file must config in top so that all file get acces to enviormtn varuible
import dotenv from 'dotenv'
// configurag and loading all envirment variable -> .env have 0 dependencies
dotenv.config();
